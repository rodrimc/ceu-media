#include "c.ceu"
#include "media/media.ceu"

var& IScene scene;
watching Scene(_) -> (&scene), 10s do
  var u64 tick;
  every (tick) in scene.tick do
    _printf("TICK: %lu\n", tick);
  end
end

escape 0;
