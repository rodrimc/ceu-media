#include "media/srt.ceu"

var&? Scene scene = spawn Scene(Size(640,480));
watching (scene) do
  await Srt(&scene.scene,
          "resources/slideshow.srt",
          Region(0, 400, 640, 80, 3),
      );
end

escape 0;
