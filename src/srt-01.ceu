#include "media/srt.ceu"

var& IScene scene;
watching Scene(Size(640,480)) -> (&scene) do
  await Srt(&scene,
          "resources/slideshow.srt",
          Region(0, 400, 640, 80, 3),
      );
end

escape 0;
